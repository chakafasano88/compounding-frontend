import React from 'react';
import Link from 'next/link';
import Navigation from '../components/Navigation';

const Home = (props) => {
    return (
        <div>
            <Navigation />
        </div>
    );
};

export default Home;